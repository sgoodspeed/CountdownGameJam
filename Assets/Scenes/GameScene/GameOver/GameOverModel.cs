using UnityEngine;

namespace Countdown
{
    /// <summary>
    /// Model for the Game Over screen. No state yet - reserved for future data
    /// (e.g. score, stats) so the MVP triad is complete from the start.
    /// </summary>
    public class GameOverModel : MonoBehaviour
    {
    }
}
