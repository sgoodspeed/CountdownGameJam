using UnityEngine;

namespace Countdown
{
    /// <summary>
    /// Model for the Main Menu. No state yet - reserved for future main menu data
    /// (e.g. selected options, unlocks) so the MVP triad is complete from the start.
    /// </summary>
    public class MainMenuModel : MonoBehaviour
    {
    }
}
